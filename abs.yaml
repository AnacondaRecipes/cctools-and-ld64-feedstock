# the conda-build parameters to use for disabling --skip-existing
build_parameters:
  - "--suppress-variables"
  - "--error-overlinking"

staging_channel_upload_target: llvm-17.0.6-stage2

channels:
  - https://staging.continuum.io/prefect/llvm-17.0.6-stage1

aggregate_check: false