# the conda-build parameters to use for disabling --skip-existing
build_parameters:
  - "--suppress-variables"
  - "--error-overlinking"

staging_channel_upload_target: llvm-17.0.6-stage1

channels:
  - llvm-17.0.6-stage0

aggregate_check: false